// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"errors"
	"fmt"
	"math"

	"github.com/facebookincubator/ent/dialect/sql"
	"github.com/facebookincubator/ent/dialect/sql/sqlgraph"
	"github.com/facebookincubator/ent/schema/field"
	"github.com/sut63/team01/ent/drugallergy"
	"github.com/sut63/team01/ent/patientinfo"
	"github.com/sut63/team01/ent/predicate"
	"github.com/sut63/team01/ent/prescription"
)

// PatientInfoQuery is the builder for querying PatientInfo entities.
type PatientInfoQuery struct {
	config
	limit      *int
	offset     *int
	order      []OrderFunc
	unique     []string
	predicates []predicate.PatientInfo
	// eager-loading edges.
	withDrugallergys        *DrugAllergyQuery
	withPatientprescription *PrescriptionQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the builder.
func (piq *PatientInfoQuery) Where(ps ...predicate.PatientInfo) *PatientInfoQuery {
	piq.predicates = append(piq.predicates, ps...)
	return piq
}

// Limit adds a limit step to the query.
func (piq *PatientInfoQuery) Limit(limit int) *PatientInfoQuery {
	piq.limit = &limit
	return piq
}

// Offset adds an offset step to the query.
func (piq *PatientInfoQuery) Offset(offset int) *PatientInfoQuery {
	piq.offset = &offset
	return piq
}

// Order adds an order step to the query.
func (piq *PatientInfoQuery) Order(o ...OrderFunc) *PatientInfoQuery {
	piq.order = append(piq.order, o...)
	return piq
}

// QueryDrugallergys chains the current query on the drugallergys edge.
func (piq *PatientInfoQuery) QueryDrugallergys() *DrugAllergyQuery {
	query := &DrugAllergyQuery{config: piq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := piq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(patientinfo.Table, patientinfo.FieldID, piq.sqlQuery()),
			sqlgraph.To(drugallergy.Table, drugallergy.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, patientinfo.DrugallergysTable, patientinfo.DrugallergysColumn),
		)
		fromU = sqlgraph.SetNeighbors(piq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryPatientprescription chains the current query on the patientprescription edge.
func (piq *PatientInfoQuery) QueryPatientprescription() *PrescriptionQuery {
	query := &PrescriptionQuery{config: piq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := piq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(patientinfo.Table, patientinfo.FieldID, piq.sqlQuery()),
			sqlgraph.To(prescription.Table, prescription.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, patientinfo.PatientprescriptionTable, patientinfo.PatientprescriptionColumn),
		)
		fromU = sqlgraph.SetNeighbors(piq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first PatientInfo entity in the query. Returns *NotFoundError when no patientinfo was found.
func (piq *PatientInfoQuery) First(ctx context.Context) (*PatientInfo, error) {
	pis, err := piq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(pis) == 0 {
		return nil, &NotFoundError{patientinfo.Label}
	}
	return pis[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (piq *PatientInfoQuery) FirstX(ctx context.Context) *PatientInfo {
	pi, err := piq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return pi
}

// FirstID returns the first PatientInfo id in the query. Returns *NotFoundError when no id was found.
func (piq *PatientInfoQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = piq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{patientinfo.Label}
		return
	}
	return ids[0], nil
}

// FirstXID is like FirstID, but panics if an error occurs.
func (piq *PatientInfoQuery) FirstXID(ctx context.Context) int {
	id, err := piq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns the only PatientInfo entity in the query, returns an error if not exactly one entity was returned.
func (piq *PatientInfoQuery) Only(ctx context.Context) (*PatientInfo, error) {
	pis, err := piq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(pis) {
	case 1:
		return pis[0], nil
	case 0:
		return nil, &NotFoundError{patientinfo.Label}
	default:
		return nil, &NotSingularError{patientinfo.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (piq *PatientInfoQuery) OnlyX(ctx context.Context) *PatientInfo {
	pi, err := piq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return pi
}

// OnlyID returns the only PatientInfo id in the query, returns an error if not exactly one id was returned.
func (piq *PatientInfoQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = piq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{patientinfo.Label}
	default:
		err = &NotSingularError{patientinfo.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (piq *PatientInfoQuery) OnlyIDX(ctx context.Context) int {
	id, err := piq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of PatientInfos.
func (piq *PatientInfoQuery) All(ctx context.Context) ([]*PatientInfo, error) {
	if err := piq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return piq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (piq *PatientInfoQuery) AllX(ctx context.Context) []*PatientInfo {
	pis, err := piq.All(ctx)
	if err != nil {
		panic(err)
	}
	return pis
}

// IDs executes the query and returns a list of PatientInfo ids.
func (piq *PatientInfoQuery) IDs(ctx context.Context) ([]int, error) {
	var ids []int
	if err := piq.Select(patientinfo.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (piq *PatientInfoQuery) IDsX(ctx context.Context) []int {
	ids, err := piq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (piq *PatientInfoQuery) Count(ctx context.Context) (int, error) {
	if err := piq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return piq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (piq *PatientInfoQuery) CountX(ctx context.Context) int {
	count, err := piq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (piq *PatientInfoQuery) Exist(ctx context.Context) (bool, error) {
	if err := piq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return piq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (piq *PatientInfoQuery) ExistX(ctx context.Context) bool {
	exist, err := piq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the query builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (piq *PatientInfoQuery) Clone() *PatientInfoQuery {
	return &PatientInfoQuery{
		config:     piq.config,
		limit:      piq.limit,
		offset:     piq.offset,
		order:      append([]OrderFunc{}, piq.order...),
		unique:     append([]string{}, piq.unique...),
		predicates: append([]predicate.PatientInfo{}, piq.predicates...),
		// clone intermediate query.
		sql:  piq.sql.Clone(),
		path: piq.path,
	}
}

//  WithDrugallergys tells the query-builder to eager-loads the nodes that are connected to
// the "drugallergys" edge. The optional arguments used to configure the query builder of the edge.
func (piq *PatientInfoQuery) WithDrugallergys(opts ...func(*DrugAllergyQuery)) *PatientInfoQuery {
	query := &DrugAllergyQuery{config: piq.config}
	for _, opt := range opts {
		opt(query)
	}
	piq.withDrugallergys = query
	return piq
}

//  WithPatientprescription tells the query-builder to eager-loads the nodes that are connected to
// the "patientprescription" edge. The optional arguments used to configure the query builder of the edge.
func (piq *PatientInfoQuery) WithPatientprescription(opts ...func(*PrescriptionQuery)) *PatientInfoQuery {
	query := &PrescriptionQuery{config: piq.config}
	for _, opt := range opts {
		opt(query)
	}
	piq.withPatientprescription = query
	return piq
}

// GroupBy used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CardNumber string `json:"cardNumber,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.PatientInfo.Query().
//		GroupBy(patientinfo.FieldCardNumber).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
//
func (piq *PatientInfoQuery) GroupBy(field string, fields ...string) *PatientInfoGroupBy {
	group := &PatientInfoGroupBy{config: piq.config}
	group.fields = append([]string{field}, fields...)
	group.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := piq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return piq.sqlQuery(), nil
	}
	return group
}

// Select one or more fields from the given query.
//
// Example:
//
//	var v []struct {
//		CardNumber string `json:"cardNumber,omitempty"`
//	}
//
//	client.PatientInfo.Query().
//		Select(patientinfo.FieldCardNumber).
//		Scan(ctx, &v)
//
func (piq *PatientInfoQuery) Select(field string, fields ...string) *PatientInfoSelect {
	selector := &PatientInfoSelect{config: piq.config}
	selector.fields = append([]string{field}, fields...)
	selector.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := piq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return piq.sqlQuery(), nil
	}
	return selector
}

func (piq *PatientInfoQuery) prepareQuery(ctx context.Context) error {
	if piq.path != nil {
		prev, err := piq.path(ctx)
		if err != nil {
			return err
		}
		piq.sql = prev
	}
	return nil
}

func (piq *PatientInfoQuery) sqlAll(ctx context.Context) ([]*PatientInfo, error) {
	var (
		nodes       = []*PatientInfo{}
		_spec       = piq.querySpec()
		loadedTypes = [2]bool{
			piq.withDrugallergys != nil,
			piq.withPatientprescription != nil,
		}
	)
	_spec.ScanValues = func() []interface{} {
		node := &PatientInfo{config: piq.config}
		nodes = append(nodes, node)
		values := node.scanValues()
		return values
	}
	_spec.Assign = func(values ...interface{}) error {
		if len(nodes) == 0 {
			return fmt.Errorf("ent: Assign called without calling ScanValues")
		}
		node := nodes[len(nodes)-1]
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(values...)
	}
	if err := sqlgraph.QueryNodes(ctx, piq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}

	if query := piq.withDrugallergys; query != nil {
		fks := make([]driver.Value, 0, len(nodes))
		nodeids := make(map[int]*PatientInfo)
		for i := range nodes {
			fks = append(fks, nodes[i].ID)
			nodeids[nodes[i].ID] = nodes[i]
		}
		query.withFKs = true
		query.Where(predicate.DrugAllergy(func(s *sql.Selector) {
			s.Where(sql.InValues(patientinfo.DrugallergysColumn, fks...))
		}))
		neighbors, err := query.All(ctx)
		if err != nil {
			return nil, err
		}
		for _, n := range neighbors {
			fk := n.patient_id
			if fk == nil {
				return nil, fmt.Errorf(`foreign-key "patient_id" is nil for node %v`, n.ID)
			}
			node, ok := nodeids[*fk]
			if !ok {
				return nil, fmt.Errorf(`unexpected foreign-key "patient_id" returned %v for node %v`, *fk, n.ID)
			}
			node.Edges.Drugallergys = append(node.Edges.Drugallergys, n)
		}
	}

	if query := piq.withPatientprescription; query != nil {
		fks := make([]driver.Value, 0, len(nodes))
		nodeids := make(map[int]*PatientInfo)
		for i := range nodes {
			fks = append(fks, nodes[i].ID)
			nodeids[nodes[i].ID] = nodes[i]
		}
		query.withFKs = true
		query.Where(predicate.Prescription(func(s *sql.Selector) {
			s.Where(sql.InValues(patientinfo.PatientprescriptionColumn, fks...))
		}))
		neighbors, err := query.All(ctx)
		if err != nil {
			return nil, err
		}
		for _, n := range neighbors {
			fk := n.patient_id
			if fk == nil {
				return nil, fmt.Errorf(`foreign-key "patient_id" is nil for node %v`, n.ID)
			}
			node, ok := nodeids[*fk]
			if !ok {
				return nil, fmt.Errorf(`unexpected foreign-key "patient_id" returned %v for node %v`, *fk, n.ID)
			}
			node.Edges.Patientprescription = append(node.Edges.Patientprescription, n)
		}
	}

	return nodes, nil
}

func (piq *PatientInfoQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := piq.querySpec()
	return sqlgraph.CountNodes(ctx, piq.driver, _spec)
}

func (piq *PatientInfoQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := piq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %v", err)
	}
	return n > 0, nil
}

func (piq *PatientInfoQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   patientinfo.Table,
			Columns: patientinfo.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: patientinfo.FieldID,
			},
		},
		From:   piq.sql,
		Unique: true,
	}
	if ps := piq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := piq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := piq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := piq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (piq *PatientInfoQuery) sqlQuery() *sql.Selector {
	builder := sql.Dialect(piq.driver.Dialect())
	t1 := builder.Table(patientinfo.Table)
	selector := builder.Select(t1.Columns(patientinfo.Columns...)...).From(t1)
	if piq.sql != nil {
		selector = piq.sql
		selector.Select(selector.Columns(patientinfo.Columns...)...)
	}
	for _, p := range piq.predicates {
		p(selector)
	}
	for _, p := range piq.order {
		p(selector)
	}
	if offset := piq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := piq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// PatientInfoGroupBy is the builder for group-by PatientInfo entities.
type PatientInfoGroupBy struct {
	config
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (pigb *PatientInfoGroupBy) Aggregate(fns ...AggregateFunc) *PatientInfoGroupBy {
	pigb.fns = append(pigb.fns, fns...)
	return pigb
}

// Scan applies the group-by query and scan the result into the given value.
func (pigb *PatientInfoGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := pigb.path(ctx)
	if err != nil {
		return err
	}
	pigb.sql = query
	return pigb.sqlScan(ctx, v)
}

// ScanX is like Scan, but panics if an error occurs.
func (pigb *PatientInfoGroupBy) ScanX(ctx context.Context, v interface{}) {
	if err := pigb.Scan(ctx, v); err != nil {
		panic(err)
	}
}

// Strings returns list of strings from group-by. It is only allowed when querying group-by with one field.
func (pigb *PatientInfoGroupBy) Strings(ctx context.Context) ([]string, error) {
	if len(pigb.fields) > 1 {
		return nil, errors.New("ent: PatientInfoGroupBy.Strings is not achievable when grouping more than 1 field")
	}
	var v []string
	if err := pigb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// StringsX is like Strings, but panics if an error occurs.
func (pigb *PatientInfoGroupBy) StringsX(ctx context.Context) []string {
	v, err := pigb.Strings(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// String returns a single string from group-by. It is only allowed when querying group-by with one field.
func (pigb *PatientInfoGroupBy) String(ctx context.Context) (_ string, err error) {
	var v []string
	if v, err = pigb.Strings(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{patientinfo.Label}
	default:
		err = fmt.Errorf("ent: PatientInfoGroupBy.Strings returned %d results when one was expected", len(v))
	}
	return
}

// StringX is like String, but panics if an error occurs.
func (pigb *PatientInfoGroupBy) StringX(ctx context.Context) string {
	v, err := pigb.String(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Ints returns list of ints from group-by. It is only allowed when querying group-by with one field.
func (pigb *PatientInfoGroupBy) Ints(ctx context.Context) ([]int, error) {
	if len(pigb.fields) > 1 {
		return nil, errors.New("ent: PatientInfoGroupBy.Ints is not achievable when grouping more than 1 field")
	}
	var v []int
	if err := pigb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// IntsX is like Ints, but panics if an error occurs.
func (pigb *PatientInfoGroupBy) IntsX(ctx context.Context) []int {
	v, err := pigb.Ints(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Int returns a single int from group-by. It is only allowed when querying group-by with one field.
func (pigb *PatientInfoGroupBy) Int(ctx context.Context) (_ int, err error) {
	var v []int
	if v, err = pigb.Ints(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{patientinfo.Label}
	default:
		err = fmt.Errorf("ent: PatientInfoGroupBy.Ints returned %d results when one was expected", len(v))
	}
	return
}

// IntX is like Int, but panics if an error occurs.
func (pigb *PatientInfoGroupBy) IntX(ctx context.Context) int {
	v, err := pigb.Int(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64s returns list of float64s from group-by. It is only allowed when querying group-by with one field.
func (pigb *PatientInfoGroupBy) Float64s(ctx context.Context) ([]float64, error) {
	if len(pigb.fields) > 1 {
		return nil, errors.New("ent: PatientInfoGroupBy.Float64s is not achievable when grouping more than 1 field")
	}
	var v []float64
	if err := pigb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// Float64sX is like Float64s, but panics if an error occurs.
func (pigb *PatientInfoGroupBy) Float64sX(ctx context.Context) []float64 {
	v, err := pigb.Float64s(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64 returns a single float64 from group-by. It is only allowed when querying group-by with one field.
func (pigb *PatientInfoGroupBy) Float64(ctx context.Context) (_ float64, err error) {
	var v []float64
	if v, err = pigb.Float64s(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{patientinfo.Label}
	default:
		err = fmt.Errorf("ent: PatientInfoGroupBy.Float64s returned %d results when one was expected", len(v))
	}
	return
}

// Float64X is like Float64, but panics if an error occurs.
func (pigb *PatientInfoGroupBy) Float64X(ctx context.Context) float64 {
	v, err := pigb.Float64(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bools returns list of bools from group-by. It is only allowed when querying group-by with one field.
func (pigb *PatientInfoGroupBy) Bools(ctx context.Context) ([]bool, error) {
	if len(pigb.fields) > 1 {
		return nil, errors.New("ent: PatientInfoGroupBy.Bools is not achievable when grouping more than 1 field")
	}
	var v []bool
	if err := pigb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// BoolsX is like Bools, but panics if an error occurs.
func (pigb *PatientInfoGroupBy) BoolsX(ctx context.Context) []bool {
	v, err := pigb.Bools(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bool returns a single bool from group-by. It is only allowed when querying group-by with one field.
func (pigb *PatientInfoGroupBy) Bool(ctx context.Context) (_ bool, err error) {
	var v []bool
	if v, err = pigb.Bools(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{patientinfo.Label}
	default:
		err = fmt.Errorf("ent: PatientInfoGroupBy.Bools returned %d results when one was expected", len(v))
	}
	return
}

// BoolX is like Bool, but panics if an error occurs.
func (pigb *PatientInfoGroupBy) BoolX(ctx context.Context) bool {
	v, err := pigb.Bool(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

func (pigb *PatientInfoGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := pigb.sqlQuery().Query()
	if err := pigb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (pigb *PatientInfoGroupBy) sqlQuery() *sql.Selector {
	selector := pigb.sql
	columns := make([]string, 0, len(pigb.fields)+len(pigb.fns))
	columns = append(columns, pigb.fields...)
	for _, fn := range pigb.fns {
		columns = append(columns, fn(selector))
	}
	return selector.Select(columns...).GroupBy(pigb.fields...)
}

// PatientInfoSelect is the builder for select fields of PatientInfo entities.
type PatientInfoSelect struct {
	config
	fields []string
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Scan applies the selector query and scan the result into the given value.
func (pis *PatientInfoSelect) Scan(ctx context.Context, v interface{}) error {
	query, err := pis.path(ctx)
	if err != nil {
		return err
	}
	pis.sql = query
	return pis.sqlScan(ctx, v)
}

// ScanX is like Scan, but panics if an error occurs.
func (pis *PatientInfoSelect) ScanX(ctx context.Context, v interface{}) {
	if err := pis.Scan(ctx, v); err != nil {
		panic(err)
	}
}

// Strings returns list of strings from selector. It is only allowed when selecting one field.
func (pis *PatientInfoSelect) Strings(ctx context.Context) ([]string, error) {
	if len(pis.fields) > 1 {
		return nil, errors.New("ent: PatientInfoSelect.Strings is not achievable when selecting more than 1 field")
	}
	var v []string
	if err := pis.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// StringsX is like Strings, but panics if an error occurs.
func (pis *PatientInfoSelect) StringsX(ctx context.Context) []string {
	v, err := pis.Strings(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// String returns a single string from selector. It is only allowed when selecting one field.
func (pis *PatientInfoSelect) String(ctx context.Context) (_ string, err error) {
	var v []string
	if v, err = pis.Strings(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{patientinfo.Label}
	default:
		err = fmt.Errorf("ent: PatientInfoSelect.Strings returned %d results when one was expected", len(v))
	}
	return
}

// StringX is like String, but panics if an error occurs.
func (pis *PatientInfoSelect) StringX(ctx context.Context) string {
	v, err := pis.String(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Ints returns list of ints from selector. It is only allowed when selecting one field.
func (pis *PatientInfoSelect) Ints(ctx context.Context) ([]int, error) {
	if len(pis.fields) > 1 {
		return nil, errors.New("ent: PatientInfoSelect.Ints is not achievable when selecting more than 1 field")
	}
	var v []int
	if err := pis.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// IntsX is like Ints, but panics if an error occurs.
func (pis *PatientInfoSelect) IntsX(ctx context.Context) []int {
	v, err := pis.Ints(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Int returns a single int from selector. It is only allowed when selecting one field.
func (pis *PatientInfoSelect) Int(ctx context.Context) (_ int, err error) {
	var v []int
	if v, err = pis.Ints(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{patientinfo.Label}
	default:
		err = fmt.Errorf("ent: PatientInfoSelect.Ints returned %d results when one was expected", len(v))
	}
	return
}

// IntX is like Int, but panics if an error occurs.
func (pis *PatientInfoSelect) IntX(ctx context.Context) int {
	v, err := pis.Int(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64s returns list of float64s from selector. It is only allowed when selecting one field.
func (pis *PatientInfoSelect) Float64s(ctx context.Context) ([]float64, error) {
	if len(pis.fields) > 1 {
		return nil, errors.New("ent: PatientInfoSelect.Float64s is not achievable when selecting more than 1 field")
	}
	var v []float64
	if err := pis.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// Float64sX is like Float64s, but panics if an error occurs.
func (pis *PatientInfoSelect) Float64sX(ctx context.Context) []float64 {
	v, err := pis.Float64s(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64 returns a single float64 from selector. It is only allowed when selecting one field.
func (pis *PatientInfoSelect) Float64(ctx context.Context) (_ float64, err error) {
	var v []float64
	if v, err = pis.Float64s(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{patientinfo.Label}
	default:
		err = fmt.Errorf("ent: PatientInfoSelect.Float64s returned %d results when one was expected", len(v))
	}
	return
}

// Float64X is like Float64, but panics if an error occurs.
func (pis *PatientInfoSelect) Float64X(ctx context.Context) float64 {
	v, err := pis.Float64(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bools returns list of bools from selector. It is only allowed when selecting one field.
func (pis *PatientInfoSelect) Bools(ctx context.Context) ([]bool, error) {
	if len(pis.fields) > 1 {
		return nil, errors.New("ent: PatientInfoSelect.Bools is not achievable when selecting more than 1 field")
	}
	var v []bool
	if err := pis.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// BoolsX is like Bools, but panics if an error occurs.
func (pis *PatientInfoSelect) BoolsX(ctx context.Context) []bool {
	v, err := pis.Bools(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bool returns a single bool from selector. It is only allowed when selecting one field.
func (pis *PatientInfoSelect) Bool(ctx context.Context) (_ bool, err error) {
	var v []bool
	if v, err = pis.Bools(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{patientinfo.Label}
	default:
		err = fmt.Errorf("ent: PatientInfoSelect.Bools returned %d results when one was expected", len(v))
	}
	return
}

// BoolX is like Bool, but panics if an error occurs.
func (pis *PatientInfoSelect) BoolX(ctx context.Context) bool {
	v, err := pis.Bool(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

func (pis *PatientInfoSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := pis.sqlQuery().Query()
	if err := pis.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (pis *PatientInfoSelect) sqlQuery() sql.Querier {
	selector := pis.sql
	selector.Select(selector.Columns(pis.fields...)...)
	return selector
}
